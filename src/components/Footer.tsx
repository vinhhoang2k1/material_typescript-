import { Typography } from "@material-ui/core";
import { Box } from "@mui/material";
import React from "react";

const Footer = () => {
  return (
    <Box textAlign='center'>
      <Typography variant="subtitle1">this is footer</Typography>
    </Box>
  );
};

export default Footer;
